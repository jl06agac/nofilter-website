# ⛔ PROTECTED ASSETS — DO NOT DELETE OR OVERWRITE WITHOUT ALEX'S EXPLICIT PERMISSION

**Standing order (Alex, 2026-07-07).** Every file listed below is protected. Claude must **never delete, overwrite, rename, move, or re-stamp over any of these without Alex's explicit, per-item say-so.** These were built or recovered by hand and have been lost once already to an unsupervised re-stamp — that must not happen again. If a build/stamp/graft would touch one of these, STOP and ask first.

If you need to change one, the rule is: **duplicate → change the copy → get Alex's sign-off → only then replace**, and keep the prior version as a dated backup. Never edit-in-place-and-hope.

---

## THE CANONICAL IMPACT SLIDER (single source of truth)

**`nf-impact-standalone.html`** — Alex's Jul 7 file. This is the **canonical impact slider** (the morph "SGD/kg = ¢/cup" builder with the durability note, thumb-pulse, and `.mgut` hover boxes). **Every impact slider on the website and in every client brochure is this component.** If the slider is ever improved, it is improved HERE first, then re-propagated. Do not let any other file's slider diverge from this one.

- **`_test-impact-slider.html`** — the exact, isolated slider extracted verbatim from the canonical file (byte-identical morph markup + engine). Reference copy for grafting.

## THE WEBSITE BUILDER (carries the canonical slider)

- **`workplace-builder.html`** — the gated Quote Builder; its price step now carries the canonical morph slider (grafted 2026-07-07). Preserves the coffee grid, machine picker, access gate, and `window.__pickedOrigins → compute()`.
- **`workplace-builder.PRE-CANON-SLIDER.html`** — pre-graft backup. Keep until Alex confirms the graft is good.

## THE CRT ACCESS SYSTEM (request access → code → CRT → builder)

- **`workplace-access.html`** — the standalone CRT access page (harvested UWCSEA CRT monitor; type the shared code → power-off → hands off to the builder).
- **`_test-crt-gate.html`** — CRT gate prototype.
- **`keystroke.mp3`** — the CRT keystroke sound (copied from `Clients/_shared/`).
- **`keystroke-audio.js`** — base64-inlined keystroke buffer so the CRT sound works on local `file://`.
- **`nofilter-counter.html`** — demo split-flap counter board used by the private-reveal tablet tile.
- **`workplace.src.html` / `workplace.html`** — the At Work page (source + stamped): the request-access section that links to `workplace-access.html`.

## THE MANIFESTO + ORIGINS PAGE (moved off the homepage)

- **`../../DO-NOT-TOUCH-Claude-files/_partials/manifesto-holdtheline.html`** — the **single source of truth** for the "Most … coffee" morph headline + ethos body (orange "matters most"). Every page that shows this manifesto includes THIS partial; edit it here, never a copy. (Careful: a partial must never contain its own `[[INCLUDE:…]]` token, even in a comment — it recurses.)
- **`origins.src.html` / `origins.html`** — the Origins page: seam immersive → manifesto → origins (four forests) → the coffees → "verified, not claimed" proof → CTA. Built from the homepage sections.
- **`index.src.PRE-ORIGINS.html`** and **`../../DO-NOT-TOUCH-Claude-files/_partials/nfbar.PRE-ORIGINS.html`** — pre-split backups. Keep until Alex confirms the Origins split is good.

## PROTOTYPES / RECOVERED WORK (keep as record)

- **`_test-private-reveal.html`** — the private "what runs behind the scenes" reveal (tablet counter, servicing self-scheduling, map, money tiles).
- **`_test-mani-squeeze.html`** — the "Most … coffee" squeeze + letter-cycle animation prototype.
- **`_test-gutter-slider.html`** — the recovered **gutter slider** (the earlier hover-side-message concept). Superseded by the morph slider above, but kept as the record of that approach.

## RECOVERED-SLIDER CANON SOURCES (elsewhere in the repo — also protected)

- `DO-NOT-TOUCH-Claude-files/_Part2-Master/_device-donor/NoFilter-Part2-MASTER-merged-WIP.html` — holds the recovered gutter slider (the newest, clamped version).
- `Clients/Schools/UWCSEA/partnership-deploy/AED-slider-ONLY.html` — the earlier gutter-slider build.

---

_Last updated 2026-07-07. If you add a new hand-built or recovered component, add it to this list._
