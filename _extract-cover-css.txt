
/* ---- from Eastspring build, style block @220732 ---- */
<style id="nf-htl-cue">
/* Downward cue — appears only once the title has converged, so it never competes with the
   stack resolving. Flashes on a slow irregular beat, then settles to a steady bob. */
.htl-cue{position:absolute;left:50%;bottom:clamp(28px,5vh,64px);transform:translateX(-50%);
  z-index:6;display:flex;flex-direction:column;align-items:center;gap:9px;
  background:none;border:0;padding:8px 14px;cursor:pointer;
  opacity:0;pointer-events:none;transition:opacity .7s ease;}
.htl-cue.on{opacity:1;pointer-events:auto;}
.htl-cue.gone{opacity:0;pointer-events:none;}
.htl-cue-lab{font-family:var(--font-m,ui-monospace,monospace);font-size:10px;letter-spacing:.16em;
  text-transform:uppercase;color:rgba(244,242,238,.62);white-space:nowrap;}
.htl-cue svg{width:26px;height:16px;display:block;overflow:visible;}
.htl-cue svg path{fill:none;stroke:var(--nf-orange,#ee4d17);stroke-width:2.4;
  stroke-linecap:square;stroke-linejoin:miter;}
.htl-cue.on svg{animation:htlCueBob 2.1s cubic-bezier(.4,0,.3,1) infinite;}
.htl-cue.on .htl-cue-lab{animation:htlCueFlash 2.1s steps(1,end) infinite;}
@keyframes htlCueBob{0%,100%{transform:translateY(0)}22%{transform:translateY(7px)}44%{transform:translateY(0)}}
@keyframes htlCueFlash{0%,58%{opacity:.62}60%,64%{opacity:.12}66%,72%{opacity:1}74%,100%{opacity:.62}}
.htl-cue:hover svg path{stroke:var(--nf-white,#fff);}
@media(prefers-reduced-motion:reduce){.htl-cue.on svg,.htl-cue.on .htl-cue-lab{animation:none;}}

/* The turn: one line, alone on the band, before any of the proposal starts. */
.nf-turn{background:#efe9dc;color:#1c1a16;
  padding:clamp(56px,8vh,96px) 24px clamp(26px,3vh,40px);}
.nf-turn-inner{max-width:min(1200px,92vw);margin:0 auto;text-align:left;}
.nf-turn p{margin:0;font-family:var(--font-display,var(--font-d));font-weight:700;
  font-size:clamp(30px,5.4vw,68px);line-height:1.04;letter-spacing:-.015em;text-wrap:balance;
  max-width:100%;margin-left:0;margin-right:0;}
/* the passage: lead stays display-weight, body drops to a reading size, hand-off points down */
.nf-turn p.nf-turn-body{font-family:var(--font-b,var(--font-body));font-weight:400;
  font-size:clamp(16px,1.5vw,21px);line-height:1.6;letter-spacing:0;
  max-width:62ch;margin:clamp(24px,3vh,36px) 0 0;color:rgba(28,26,22,.78);}
.nf-turn p.nf-turn-hand{font-family:var(--font-m,monospace);font-weight:500;
  font-size:clamp(11px,1.05vw,13px);line-height:1.5;letter-spacing:.13em;text-transform:uppercase;
  max-width:62ch;margin:clamp(18px,2.2vh,26px) 0 0;color:rgba(28,26,22,.5);}
.nf-turn .u{color:var(--nf-orange,#ee4d17);}
.nf-turn p{opacity:0;transform:translateY(18px);transition:opacity .9s ease,transform .9s cubic-bezier(.16,1,.3,1);}
.nf-turn.in p{opacity:1;transform:none;}
.nf-turn-grid{display:grid;grid-template-columns:1.12fr .88fr;gap:clamp(34px,5vw,72px);align-items:start;margin-top:clamp(26px,3.5vh,44px);}
.nf-turn-story{max-width:58ch;}
.nf-turn-media{display:flex;flex-direction:column;gap:14px;opacity:0;transform:translateY(20px);transition:opacity 1s ease .25s,transform 1s cubic-bezier(.16,1,.3,1) .25s;}
.nf-turn.in .nf-turn-media{opacity:1;transform:none;}
.ntm-clip{position:relative;margin:0;border-radius:12px;overflow:hidden;aspect-ratio:16/9;box-shadow:0 18px 42px rgba(28,26,22,.18);}
.ntm-clip video{width:100%;height:100%;object-fit:cover;display:block;}
.ntm-clip::after{content:"";position:absolute;inset:0;z-index:1;background:linear-gradient(180deg,transparent 52%,rgba(10,8,6,.62));}
.ntm-clip figcaption{position:absolute;left:14px;bottom:11px;z-index:2;font-family:var(--font-m,monospace);font-size:10px;letter-spacing:.16em;text-transform:uppercase;color:#fff;}
@media(max-width:820px){.nf-turn-grid{grid-template-columns:1fr;}}
/* stagger the three so the passage arrives as a thought, not a block */
.nf-turn p.nf-turn-body{transition-delay:.22s;}
.nf-turn p.nf-turn-hand{transition-delay:.44s;}
@media(max-width:640px){.nf-turn p{max-width:none;}}
</style>
/* ---- from Eastspring build, style block @224687 ---- */
<style id="nf-hero-website">
/* Website hero CSS, lifted whole so the cover behaves exactly as it does on the site. */
.cover-hero-title,.stance-statement,.choice-headline{text-wrap:balance;}
.cover{height:100vh;min-height:560px;position:relative;overflow:hidden;
  background:var(--nf-near-black);color:#fff;display:flex;align-items:center;justify-content:center;text-align:center;}
.cover-video,.cover-bg{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:0;}
/* Torn seam at cover bottom — dashed-red overlay tracing the same vertices as
   the video clip, so the rip reads as a single device (matches Progress Report cover). */
.cover-tear{display:none;}
.cover-bg{position:absolute;inset:0;z-index:1;pointer-events:none;
  background:linear-gradient(180deg,rgba(26,24,21,.55) 0%,rgba(26,24,21,.30) 45%,rgba(26,24,21,.72) 100%);}
.cover-inner{position:relative;z-index:2;display:flex;flex-direction:column;align-items:center;padding:0 24px;width:100%;}
.cover-hero-title{font-family:var(--font-display);font-weight:900;
  font-size:clamp(64px,14vw,240px);line-height:.86;text-transform:uppercase;
  letter-spacing:var(--tk-display-tight);color:var(--nf-white);margin:0;position:relative;
  text-shadow:0 4px 30px rgba(0,0,0,.45);}
.cover-hero-title.cascade{display:flex;flex-direction:column;align-items:stretch;
  width:100%;margin:0 auto;line-height:.74;gap:.01em;text-align:center;transform:translateX(-1.5vw) translateY(4vh) scale(0.9);transform-origin:center;}
.cover-hero-title.cascade .line-row{display:block;position:relative;padding:.04em 0;width:100%;
  margin-left:-.02em;overflow:visible;}
/* glitch-materialise: lines are placed, not swooped — they flicker/slice into being */
.cover-hero-title .rise{display:block;opacity:0;}
.cover-hero-title.revealed .r1 .rise{animation:lineMaterialise .82s steps(2,end) .00s both;}
.cover-hero-title.revealed .r2 .rise{animation:lineMaterialise .82s steps(2,end) .36s both;}
.cover-hero-title.revealed .r3 .rise{animation:lineMaterialise .82s steps(2,end) .72s both;}
@keyframes lineMaterialise{
  0%{opacity:0;transform:translate(0,0);clip-path:inset(45% 0 45% 0);}
  15%{opacity:.7;transform:translate(-4px,1px);clip-path:inset(0 0 62% 0);}
  28%{opacity:.25;transform:translate(5px,-2px);clip-path:inset(40% 0 22% 0);}
  42%{opacity:.85;transform:translate(-3px,0);clip-path:inset(55% 0 0 0);}
  55%{opacity:.4;transform:translate(3px,1px);clip-path:none;}
  70%{opacity:.95;transform:translate(-1px,0);}
  100%{opacity:1;transform:translate(0,0);clip-path:none;}
}
@media(prefers-reduced-motion:reduce){
  .cover-hero-title .rise{opacity:1;animation:none;}
}
/* [PREVIEW] retain the original glitch-materialise animation, but PLACE it one word at a time (my placement device) */
.cover-hero-title .rise.place{animation:lineMaterialise .82s steps(2,end) both;}
/* "the" (r2) lands more deliberately — a longer, weightier materialise that then holds */
.cover-hero-title .r2 .rise.place{animation-duration:1.2s;animation-timing-function:steps(3,end);}
/* THE is the pivot — dead center. HOLD left, LINE right — tuned for equal edge gaps at 1440.
   HOLD nudged down + LINE up by ~10px to tighten the vertical gap around THE. */
.cover-hero-title.cascade .r1{transform:translateX(calc(-22.6vw + 5px)) translateY(10px);}
.cover-hero-title.cascade .r2{transform:translateX(-12px);}
.cover-hero-title.cascade .r3{transform:translateX(21.8vw) translateY(-10px);}
.cover-hero-title .o{color:var(--nf-orange);}
@media(max-width:900px){
  .cover-hero-title.cascade .r1{transform:translateX(-15vw);}
  .cover-hero-title.cascade .r3{transform:translateX(17vw);}
}
@media(max-width:600px){
  .cover-hero-title.cascade .r1{transform:translateX(-11vw);}
  .cover-hero-title.cascade .r3{transform:translateX(13vw);}
}
/* headline — present on load, no entrance animation */
.cover-hero-title .glitch{position:relative;display:inline-block;}
.cover-hero-title .glitch::before,
.cover-hero-title .glitch::after{
  content:attr(data-text);position:absolute;top:0;left:0;width:100%;height:100%;
  opacity:0;pointer-events:none;text-transform:uppercase;color:#fff;}
.cover-hero-title .glitch.is-glitching::before{
  color:#00e5ff;mix-blend-mode:screen;animation:glitchTop .6s steps(2,end) 1;}
.cover-hero-title .glitch.is-glitching::after{
  color:#ff003c;mix-blend-mode:screen;animation:glitchBot .6s steps(2,end) 1;}
.cover-hero-title .glitch.is-glitching{animation:glitchMain .6s steps(2,end) 1;}
/* LINE under threat: shudders + slice-fragments, but never lost — reads as besieged, not broken */
@keyframes glitchMain{
  0%{transform:none;opacity:1;clip-path:none;}
  8%{transform:translate(-3px,1px);opacity:.9;clip-path:inset(0 0 60% 0);}
  14%{transform:translate(4px,-1px);opacity:.62;clip-path:inset(30% 0 20% 0);}
  20%{transform:translate(-2px,0);opacity:.95;clip-path:none;}
  30%{transform:translate(3px,1px);opacity:.66;clip-path:inset(0 0 75% 0);}
  38%{transform:none;opacity:.58;}
  44%{transform:translate(-2px,-1px);opacity:.97;clip-path:inset(55% 0 0 0);}
  58%{transform:translate(1px,1px);opacity:.8;clip-path:none;}
  70%{transform:none;opacity:1;}
  100%{transform:none;opacity:1;clip-path:none;}
}
@keyframes glitchTop{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.55;transform:translate(-4px,-2px);clip-path:inset(0 0 68% 0);}
  16%{opacity:.5;transform:translate(5px,1px);clip-path:inset(0 0 40% 0);}
  30%{opacity:.55;transform:translate(-4px,-1px);clip-path:inset(45% 0 0 0);}
  44%{opacity:.45;transform:translate(4px,2px);clip-path:inset(0 0 72% 0);}
  58%{opacity:.3;transform:translate(-2px,0);}
  70%{opacity:0;}100%{opacity:0;}
}
@keyframes glitchBot{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.55;transform:translate(4px,2px);clip-path:inset(62% 0 0 0);}
  16%{opacity:.5;transform:translate(-5px,-1px);clip-path:inset(33% 0 33% 0);}
  30%{opacity:.55;transform:translate(4px,1px);clip-path:inset(0 0 58% 0);}
  44%{opacity:.45;transform:translate(-4px,-2px);clip-path:inset(52% 0 0 0);}
  58%{opacity:.3;transform:translate(3px,0);}
  70%{opacity:0;}100%{opacity:0;}
}

/* ── hero image fragility: the LINE slice device, applied to the video ──
   Base layer tears into displaced bands (mirrors glitchMain). Two copies flash
   as cyan/red offset slices (mirror glitchTop/glitchBot), screen-blended.
   Copies are invisible until glitch, so they cost nothing at rest. */
.cover-video-stack{position:absolute;inset:0;z-index:0;}
.cover-video--cyan,.cover-video--red{opacity:0;mix-blend-mode:screen;}
.cover-video.is-glitching{animation:vidGlitchMain .6s steps(2,end) 1;}
.cover-video--cyan.is-glitching{animation:vidGlitchTop .6s steps(2,end) 1;}
.cover-video--red.is-glitching{animation:vidGlitchBot .6s steps(2,end) 1;}
.cover-video--cyan{background:#00e5ff;}
.cover-video--red{background:#ff003c;}
@keyframes vidGlitchMain{
  0%{transform:none;clip-path:none;}
  8%{transform:translate(-2px,1px);clip-path:inset(0 0 60% 0);}
  14%{transform:translate(3px,-1px);clip-path:inset(30% 0 20% 0);}
  20%{transform:translate(-1px,0);clip-path:none;}
  30%{transform:translate(2px,1px);clip-path:inset(0 0 75% 0);}
  38%{transform:none;}
  44%{transform:translate(-2px,-1px);clip-path:inset(55% 0 0 0);}
  58%{transform:translate(1px,1px);clip-path:none;}
  70%{transform:none;}100%{transform:none;clip-path:none;}
}
@keyframes vidGlitchTop{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.32;transform:translate(-4px,-2px);clip-path:inset(0 0 68% 0);}
  16%{opacity:.28;transform:translate(5px,1px);clip-path:inset(0 0 40% 0);}
  30%{opacity:.32;transform:translate(-3px,-1px);clip-path:inset(45% 0 0 0);}
  44%{opacity:.24;transform:translate(4px,2px);clip-path:inset(0 0 72% 0);}
  58%{opacity:.16;transform:translate(-2px,0);}
  70%{opacity:0;}100%{opacity:0;}
}
@keyframes vidGlitchBot{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.32;transform:translate(4px,2px);clip-path:inset(62% 0 0 0);}
  16%{opacity:.28;transform:translate(-5px,-1px);clip-path:inset(33% 0 33% 0);}
  30%{opacity:.32;transform:translate(3px,1px);clip-path:inset(0 0 58% 0);}
  44%{opacity:.24;transform:translate(-4px,-2px);clip-path:inset(52% 0 0 0);}
  58%{opacity:.16;transform:translate(2px,0);}
  70%{opacity:0;}100%{opacity:0;}
}
/* ── HTL battle: the mega-farm intercuts the hero, holds, then recedes to pure hero (--mega) ── */
.cover-mega{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:0;opacity:var(--mega,1);filter:grayscale(.7) contrast(1.15) brightness(.88) sepia(.35) hue-rotate(165deg) saturate(1.1);}
.cover.mega-tearing .cover-mega{animation:coverMegaTear .5s steps(3) infinite;}
@keyframes coverMegaTear{
  0%{clip-path:inset(0 0 88% 0);transform:translate(-1%,0);}
  25%{clip-path:inset(42% 0 44% 0);transform:translate(1.4%,0);}
  50%{clip-path:inset(70% 0 16% 0);transform:translate(-1.4%,0);}
  75%{clip-path:inset(18% 0 66% 0);transform:translate(1%,0);}
  100%{clip-path:inset(0 0 0 0);transform:translate(0,0);}
}
@media(prefers-reduced-motion:reduce){.cover.mega-tearing .cover-mega{animation:none;}}
</style>
/* ---- from Eastspring build, style block @331559 ---- */
<style id="nf-hero-holdtheline">
.cover-hero-title,.stance-statement,.choice-headline{text-wrap:balance;}
.cover{height:100vh;min-height:560px;position:relative;overflow:hidden;
  background:var(--nf-near-black);color:#fff;display:flex;align-items:center;justify-content:center;text-align:center;}
.cover-video,.cover-bg{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:0;}
/* Torn seam at cover bottom — dashed-red overlay tracing the same vertices as
   the video clip, so the rip reads as a single device (matches Progress Report cover). */
.cover-tear{display:none;}
.cover-bg{position:absolute;inset:0;z-index:1;pointer-events:none;
  background:linear-gradient(180deg,rgba(26,24,21,.55) 0%,rgba(26,24,21,.30) 45%,rgba(26,24,21,.72) 100%);}
.cover-inner{position:relative;z-index:2;display:flex;flex-direction:column;align-items:center;padding:0 24px;width:100%;}
.cover-hero-title{font-family:var(--font-display);font-weight:900;
  font-size:clamp(64px,14vw,240px);line-height:.86;text-transform:uppercase;
  letter-spacing:var(--tk-display-tight);color:var(--nf-white);margin:0;position:relative;
  text-shadow:0 4px 30px rgba(0,0,0,.45);}
.cover-hero-title.cascade{display:flex;flex-direction:column;align-items:stretch;
  width:100%;margin:0 auto;line-height:.82;gap:.06em;text-align:center;transform:translateX(-1.5vw) translateY(4vh) scale(0.9);transform-origin:center;}
.cover-hero-title.cascade .line-row{display:block;position:relative;padding:.04em 0;width:100%;
  margin-left:-.02em;overflow:visible;}
/* glitch-materialise: lines are placed, not swooped — they flicker/slice into being */
.cover-hero-title .rise{display:block;opacity:0;}
.cover-hero-title.revealed .r1 .rise{animation:lineMaterialise .82s steps(2,end) .00s both;}
.cover-hero-title.revealed .r2 .rise{animation:lineMaterialise .82s steps(2,end) .36s both;}
.cover-hero-title.revealed .r3 .rise{animation:lineMaterialise .82s steps(2,end) .72s both;}
@keyframes lineMaterialise{
  0%{opacity:0;transform:translate(0,0);clip-path:inset(45% 0 45% 0);}
  15%{opacity:.7;transform:translate(-4px,1px);clip-path:inset(0 0 62% 0);}
  28%{opacity:.25;transform:translate(5px,-2px);clip-path:inset(40% 0 22% 0);}
  42%{opacity:.85;transform:translate(-3px,0);clip-path:inset(55% 0 0 0);}
  55%{opacity:.4;transform:translate(3px,1px);clip-path:none;}
  70%{opacity:.95;transform:translate(-1px,0);}
  100%{opacity:1;transform:translate(0,0);clip-path:none;}
}
@media(prefers-reduced-motion:reduce){
  .cover-hero-title .rise{opacity:1;animation:none;}
}
/* THE is the pivot — dead center. HOLD left, LINE right — tuned for equal edge gaps at 1440.
   HOLD nudged down + LINE up by ~10px to tighten the vertical gap around THE. */
/* Once the three words converge onto one line, the stacked-layout nudges are wrong:
   they leave the finished line ~25px left of centre. Measured, not eyeballed. */
.cover-hero-title.cascade.converged{transform:translateX(0) translateY(4vh) scale(0.9);}
.cover-hero-title.cascade.converged .line-row{margin-left:0;}
.cover-hero-title.cascade .r1{transform:translateX(calc(-22.6vw + 5px)) translateY(10px);}
.cover-hero-title.cascade .r2{transform:translateX(-12px);}
.cover-hero-title.cascade .r3{transform:translateX(21.8vw) translateY(-10px);}
.cover-hero-title .o{color:var(--nf-orange);}
@media(max-width:900px){
  .cover-hero-title.cascade .r1{transform:translateX(-15vw);}
  .cover-hero-title.cascade .r3{transform:translateX(17vw);}
}
@media(max-width:600px){
  .cover-hero-title.cascade .r1{transform:translateX(-11vw);}
  .cover-hero-title.cascade .r3{transform:translateX(13vw);}
}
/* headline — present on load, no entrance animation */
.cover-hero-title .glitch{position:relative;display:inline-block;}
.cover-hero-title .glitch::before,
.cover-hero-title .glitch::after{
  content:attr(data-text);position:absolute;top:0;left:0;width:100%;height:100%;
  opacity:0;pointer-events:none;text-transform:uppercase;color:#fff;}
.cover-hero-title .glitch.is-glitching::before{
  color:#00e5ff;mix-blend-mode:screen;animation:glitchTop .6s steps(2,end) 1;}
.cover-hero-title .glitch.is-glitching::after{
  color:#ff003c;mix-blend-mode:screen;animation:glitchBot .6s steps(2,end) 1;}
.cover-hero-title .glitch.is-glitching{animation:glitchMain .6s steps(2,end) 1;}
/* LINE under threat: opacity dropouts + slice-fragments — nearly evaporates, then holds */
@keyframes glitchMain{
  0%{transform:none;opacity:1;clip-path:none;}
  8%{transform:translate(-5px,1px);opacity:.85;clip-path:inset(0 0 60% 0);}
  14%{transform:translate(6px,-2px);opacity:.25;clip-path:inset(30% 0 20% 0);}   /* nearly gone */
  20%{transform:translate(-3px,0);opacity:.9;clip-path:none;}
  30%{transform:translate(4px,1px);opacity:.4;clip-path:inset(0 0 75% 0);}        /* fragment */
  38%{transform:none;opacity:.15;}                                               /* almost evaporated */
  44%{transform:translate(-4px,-1px);opacity:.95;clip-path:inset(55% 0 0 0);}     /* snaps back */
  58%{transform:translate(2px,1px);opacity:.5;clip-path:none;}
  70%{transform:none;opacity:1;}                                                  /* holds */
  100%{transform:none;opacity:1;clip-path:none;}
}
@keyframes glitchTop{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.95;transform:translate(-8px,-3px);clip-path:inset(0 0 68% 0);}
  16%{opacity:.9;transform:translate(10px,2px);clip-path:inset(0 0 40% 0);}
  30%{opacity:.95;transform:translate(-7px,-1px);clip-path:inset(45% 0 0 0);}
  44%{opacity:.8;transform:translate(8px,3px);clip-path:inset(0 0 72% 0);}
  58%{opacity:.5;transform:translate(-4px,0);}
  70%{opacity:0;}100%{opacity:0;}
}
@keyframes glitchBot{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.95;transform:translate(8px,3px);clip-path:inset(62% 0 0 0);}
  16%{opacity:.9;transform:translate(-10px,-2px);clip-path:inset(33% 0 33% 0);}
  30%{opacity:.95;transform:translate(7px,2px);clip-path:inset(0 0 58% 0);}
  44%{opacity:.8;transform:translate(-8px,-3px);clip-path:inset(52% 0 0 0);}
  58%{opacity:.5;transform:translate(5px,0);}
  70%{opacity:0;}100%{opacity:0;}
}

/* ── hero image fragility: the LINE slice device, applied to the video ──
   Base layer tears into displaced bands (mirrors glitchMain). Two copies flash
   as cyan/red offset slices (mirror glitchTop/glitchBot), screen-blended.
   Copies are invisible until glitch, so they cost nothing at rest. */
.cover-video-stack{position:absolute;inset:0;z-index:0;}
.cover-video--cyan,.cover-video--red{opacity:0;mix-blend-mode:screen;}
.cover-video.is-glitching{animation:vidGlitchMain .6s steps(2,end) 1;}
.cover-video--cyan.is-glitching{animation:vidGlitchTop .6s steps(2,end) 1;}
.cover-video--red.is-glitching{animation:vidGlitchBot .6s steps(2,end) 1;}
.cover-video--cyan{background:#00e5ff;}
.cover-video--red{background:#ff003c;}
@keyframes vidGlitchMain{
  0%{transform:none;clip-path:none;}
  8%{transform:translate(-2px,1px);clip-path:inset(0 0 60% 0);}
  14%{transform:translate(3px,-1px);clip-path:inset(30% 0 20% 0);}
  20%{transform:translate(-1px,0);clip-path:none;}
  30%{transform:translate(2px,1px);clip-path:inset(0 0 75% 0);}
  38%{transform:none;}
  44%{transform:translate(-2px,-1px);clip-path:inset(55% 0 0 0);}
  58%{transform:translate(1px,1px);clip-path:none;}
  70%{transform:none;}100%{transform:none;clip-path:none;}
}
@keyframes vidGlitchTop{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.32;transform:translate(-4px,-2px);clip-path:inset(0 0 68% 0);}
  16%{opacity:.28;transform:translate(5px,1px);clip-path:inset(0 0 40% 0);}
  30%{opacity:.32;transform:translate(-3px,-1px);clip-path:inset(45% 0 0 0);}
  44%{opacity:.24;transform:translate(4px,2px);clip-path:inset(0 0 72% 0);}
  58%{opacity:.16;transform:translate(-2px,0);}
  70%{opacity:0;}100%{opacity:0;}
}
@keyframes vidGlitchBot{
  0%{opacity:0;clip-path:inset(0 0 0 0);}
  8%{opacity:.32;transform:translate(4px,2px);clip-path:inset(62% 0 0 0);}
  16%{opacity:.28;transform:translate(-5px,-1px);clip-path:inset(33% 0 33% 0);}
  30%{opacity:.32;transform:translate(3px,1px);clip-path:inset(0 0 58% 0);}
  44%{opacity:.24;transform:translate(-4px,-2px);clip-path:inset(52% 0 0 0);}
  58%{opacity:.16;transform:translate(2px,0);}
  70%{opacity:0;}100%{opacity:0;}
}
@media(prefers-reduced-motion:reduce){
  .cover-video.is-glitching,.cover-video--cyan.is-glitching,.cover-video--red.is-glitching{animation:none;}
</style>
/* ---- from Eastspring build, style block @1795820 ---- */
<style id="htl-travel-css">
/* ═══ HOLD THE LINE — one gesture, not a gesture plus a correction ════════════
   Sampled frame by frame, the converge was originally a SINGLE-frame cut: row 1
   moved 116px across and 184px down between one frame and the next, because
   transition-duration computed to 0s on .line-row. Giving the rows a real
   transition fixed the travel but exposed a second problem — the optical centring
   ran 900ms LATER and shifted the whole lockup sideways once the words had already
   stopped. Under scheme B that shift is half the HOLD/LINE width difference rather
   than a few pixels of bearing trim, so it read as the thing correcting itself.
   The destination is now measured before anything paints, folded into the travel,
   and the words fly straight to their final anchored positions. The corrector still
   runs afterwards, but finds nothing left to do. */
.cover-hero-title.cascade.htl-nofx,
.cover-hero-title.cascade.htl-nofx .line-row{transition:none !important;}
.cover-hero-title.cascade.htl-travel,
.cover-hero-title.cascade.htl-travel .line-row{
  transition:transform .86s cubic-bezier(.5,0,.15,1);
  will-change:transform;
}
@media(prefers-reduced-motion:reduce){
  .cover-hero-title.cascade.htl-travel,
  .cover-hero-title.cascade.htl-travel .line-row{transition:none;will-change:auto;}
}
</style>
/* ---- from Eastspring build, style block @1801627 ---- */
<style id="cue-glow-css">
/* ═══ Wayfinding chevrons ═════════════════════════════════════════════════════
   The cover's cue was dead on arrival: after the rules that give it a bob and a
   flashing label, a later rule said
       .htl-cue.on svg,.htl-cue.on .htl-cue-lab{animation:none;}
   — which cancels both, and only ever applies once the cue is switched ON. So the
   one state where it was meant to move was the one state where it could not. It is
   restored here and given the glow, so once HOLD THE LINE lands there is something
   burning at the bottom of the screen telling her where to go.
   The eight beat cues were 14px strokes in --nw-dim, the same colour as body copy
   at a third the size; they now carry the beat's accent and the same halo, scaled
   down so the cover still leads. */

/* — cover — */
.htl-cue.on svg{animation:htlCueBob 2.1s cubic-bezier(.4,0,.3,1) infinite;}
.htl-cue.on .htl-cue-lab{animation:htlCueGlowLab 2.1s ease-in-out infinite;}
.htl-cue svg{width:32px;height:20px;}
.htl-cue svg path{stroke-width:2.8;
  filter:drop-shadow(0 0 9px rgba(237,51,38,.9)) drop-shadow(0 0 22px rgba(237,51,38,.5));}
.htl-cue.on svg path{animation:htlCueGlow 2.1s ease-in-out infinite;}
.htl-cue-lab{font-size:10.5px;color:rgba(244,242,238,.82);}
@keyframes htlCueGlow{
  0%,100%{filter:drop-shadow(0 0 7px rgba(237,51,38,.75)) drop-shadow(0 0 18px rgba(237,51,38,.32));}
  50%    {filter:drop-shadow(0 0 14px rgba(237,51,38,1))  drop-shadow(0 0 34px rgba(237,51,38,.6));}
}
@keyframes htlCueGlowLab{0%,100%{opacity:.6;}50%{opacity:1;}}

/* — the eight beats — */
.nw-cue{font-size:10px;color:var(--nw-acc);}
.nw-cue svg{width:19px;height:25px;color:var(--nw-acc);stroke:currentColor;stroke-width:1.9;
  animation:nwbob 1.8s ease-in-out infinite, nwCueGlow 2.4s ease-in-out infinite;}
@keyframes nwCueGlow{
  0%,100%{filter:drop-shadow(0 0 4px currentColor);opacity:.72;}
  50%    {filter:drop-shadow(0 0 12px currentColor);opacity:1;}
}

/* — the record's hand-off button — */
.rc-cta-arrow{display:inline-flex;margin-left:9px;}
.rc-cta-arrow svg{width:13px;height:18px;fill:none;stroke:currentColor;stroke-width:2.2;
  stroke-linecap:round;stroke-linejoin:round;animation:rcCtaDrop 1.9s ease-in-out infinite;}
@keyframes rcCtaDrop{0%,100%{transform:translateY(0);opacity:.72;}50%{transform:translateY(3px);opacity:1;}}

@media(prefers-reduced-motion:reduce){
  .htl-cue.on svg,.htl-cue.on .htl-cue-lab,.htl-cue.on svg path,
  .nw-cue svg,.rc-cta-arrow svg{animation:none;}
  .htl-cue-lab{opacity:1;}
}
</style>